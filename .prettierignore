# Prettier is here to fill Biome's gaps — Markdown, YAML, and .astro files,
# since Biome doesn't parse the Astro template format at all and already
# owns every plain JS/TS/JSON file. Everything below is either somebody
# else's file or a format Prettier has no business reformatting.
node_modules/
.bun-cache/
bun.lock

# Written by the release job, not by hand.
CHANGELOG.md

# Vale's downloaded style packages.
styles/Google/
styles/proselint/

# `openspec init`'s generated skill/command docs — somebody else's docs,
# held to somebody else's rules.
.claude/
